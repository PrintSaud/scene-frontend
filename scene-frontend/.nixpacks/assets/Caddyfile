:{$PORT}

# quick proof endpoint
handle /api/health {
  respond "api-proxy-ok" 200
}

# proxy API to backend
handle_path /api/* {
  reverse_proxy https://backend.scenesa.com
}

# serve SPA
handle {
  root * /app/dist
  try_files {path} /index.html
  file_server
}